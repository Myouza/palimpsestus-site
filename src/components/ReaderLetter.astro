---
interface Props {
  author: string;
  date?: string;
}

const { author, date } = Astro.props;
---

<div class="reader-letter">
  <div><slot /></div>
  <div class="reader-letter-meta">
    —— {author}
    {date && <span>，{date}</span>}
  </div>
</div>
